/*
    Picker component 
    - allows user to select pre configured patterns and populate them to grid
    - later add functionality to allow user to save their own patterns
*/
import React from 'react'

type Props = {}

const Picker = (props: Props) => {
  return (
    <div>Picker</div>
  )
}

export default Picker